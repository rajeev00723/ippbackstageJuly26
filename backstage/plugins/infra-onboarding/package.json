{
  "name": "@internal/backstage-plugin-infra-onboarding",
  "version": "0.1.0",
  "private": true,
  "description": "Platform Engineering — Infrastructure Onboarding Frontend Plugin",
  "backstage": {
    "role": "frontend-plugin"
  },
  "main": "src/index.ts",
  "scripts": {
    "start": "backstage-cli package start",
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "type-check": "tsc"
  },
  "dependencies": {
    "react": "^18.0.2",
    "react-dom": "^18.0.2",
    "react-router-dom": "^6.3.0"
  },
  "devDependencies": {
    "@backstage/cli": "^0.26.4",
    "typescript": "~5.4.0"
  }
}
